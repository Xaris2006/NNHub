#pragma once

#include "armadillo"

namespace NN
{
	using Mat = arma::fmat;
	using Vec = arma::fvec;
	using ColVec = arma::fcolvec;
	using RowVec = arma::frowvec;
	using Cube = arma::fcube;

	using RealType = float;
}